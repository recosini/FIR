version: '2'

services:
  webapp:
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    command: ./run_fir.sh
    volumes:
      - .:/app/FIR
    expose:
      - "8000"
    links:
      - db
      - redis
    depends_on:
      - db
      - redis
    env_file:
      - ./mysql.env

  nginx:
    restart: always
    build:
      context: nginx
      dockerfile: Dockerfile
    volumes:
      - ./static:/www/static
    volumes_from:
      - webapp
    ports:
      - "8080:8080"
    links:
      - webapp:webapp
    depends_on:
      - webapp

  db:
    image: mysql:5.7
    volumes:
      - mysqldata:/var/lib/mysql
    expose:
      - "3306"
    env_file:
      - ./mysql.env

  redis:
    image: redis:3.2.5
    volumes:
      - redisdata:/var/lib/redis/data
    expose:
      - "6379"

  workers:
    build:
      context: .
      dockerfile: Dockerfile
    command: ./run_celery.sh
    volumes:
      - .:/app/FIR
    links:
      - redis
    depends_on:
      - redis

volumes:
    redisdata:
    mysqldata:
